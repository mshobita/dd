# test-definition.yaml
metadata:
  name: shell-script-test
  description: "A simple test that runs a shell script"
  format: "Lava-Test Test Definition 1.0"
  maintainer: your.email@example.com
run:
  steps:
    - chmod +x test-script.sh
    - sh test-script.sh

